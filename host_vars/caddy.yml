---
# Caddy LXC — dProxy VLAN 50
# VMID: 150 on PVE01
# IP assigned via Unifi DHCP host reservation

caddy_nameserver: "172.30.50.1"
caddy_search_domain: "lab.local"

# Upstream services Caddy will proxy to
caddy_sites:
  - domain: "proxmox.lab.local"
    upstream: "https://172.30.30.1:8006"
    tls_internal: true

  - domain: "nas.lab.local"
    upstream: "http://172.30.20.10:5000"
    tls_internal: true

  # - domain: "splunk.lab.local"
  #   upstream: "https://172.30.30.20:8000"
  #   tls_internal: true

# Optional: NFS mount from uNAS on dStorage (VLAN 20)
caddy_nas_mounts: []
  # - src: "172.30.20.10:/volume1/web"
  #   dest: "/srv/www"
  #   fstype: "nfs"
  #   opts: "ro,soft,timeo=5"