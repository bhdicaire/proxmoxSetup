---
# host_vars/caddy.yml — Caddy LXC on dProxy VLAN 50
# VMID: 150 on PVE-01
# IP: 172.30.50.30 (Unifi DHCP reservation)

caddy_nameserver: "172.30.50.1"
caddy_search_domain: "lab.local"

# ACME / Cloudflare
caddy_acme_email: "code@dicaire.com"
caddy_domain: "d5e.dev"

# Upstream services Caddy will proxy
caddy_sites:
  - subdomain: proxmox
    upstream: "https://172.30.30.11:8006"
    tls_insecure: true

  # - subdomain: proxmox2
  #   upstream: "https://172.30.30.12:8006"
  #   tls_insecure: true

  # - subdomain: nas
  #   upstream: "http://172.30.20.10:5000"
  #   tls_insecure: false

  # - subdomain: splunk
  #   upstream: "https://172.30.30.20:8000"
  #   tls_insecure: true

# Optional: NFS mounts from uNAS on dStorage (VLAN 20)
caddy_nas_mounts: []
  # - src: "172.30.20.10:/volume1/web"
  #   dest: "/srv/www"
  #   fstype: "nfs"
  #   opts: "ro,soft,timeo=5"
